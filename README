WEB
Sur votre machine
	sudo nano /etc/hosts
Après la liste des ips, ajouter
	la bonne ip	hotdogquiz.fr

Sur le serveur
	cd /etc/apache2/sites-available
	sudo cp 000-default.conf hotdogquiz.conf
	sudo nano hotdogquiz.conf

	Remplacer
		ServerAdmin webmaster@localhost
	par
		ServerName hotdogquiz.fr

	Remplacer 
		DocumentRoot /var/www/html
	par
		DocumentRoot /var/www/html/hotdogquiz

	Quitter nano en enregistrant
	sudo a2ensite hotdogquiz.conf
	sudo service apache2 restart

	cd /var/www/html/
	sudo ln -s "le/chemin/vers/le/dossier/du/projet/Web" hotdogquiz

	cd "le/chemin/vers/le/dossier/du/projet/Web"
	chmod 777 files/img/
Dans les fichiers du site
	Dans php/bddConnect.inc
	Changer la valeur, dans le if et dans le else de BDD_HOST pour lier à votre base de données IP:PORT

CPP
Dans le dossier Cpp, taper uniquement
	make qt
	make qtInstall && ./Cpp

BDD
Dans le dossier BDD
	Créer un utilisateur
		hotdogquiz
	avec comme mot de passe
		hotdogquiz
	et ayant une base
		hotdogquiz
	et tous les droits sur la table

	bdd.sql
